<% if !@list.nil? %>
  var htmlStr = "";
  <% @list.each do |user| %>
    htmlStr += "<%= j render partial: "shared/user_bar", locals: { user: user } %>";
  <% end %>
  $("#list_<%="#{@type}"%> .users-list").html(htmlStr);
<% end %>
