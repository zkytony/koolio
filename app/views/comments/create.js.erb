// comment is created. Now display the new comment.
$("#comment_list").append(
  "<%= j render partial: "shared/comment", locals: { content: "#{@comment.content}", n_likes: "#{@comment.likes}", author_name: "#{@comment.user.username}" } %>"
);
